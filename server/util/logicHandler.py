"""Deprecated — do not import from this module.

The hourly computation logic that was stubbed here now lives in
``core/history.py`` (pure) and ``util/fetchHandler.py`` (Mongo-backed
orchestration).  This file is kept only to avoid breaking old imports
that may reference it; it intentionally has no functional code.
"""
